name: 'Pakage version update and commit'
description: 'Pakage version update and commit'
inputs:
  what-to-do:  # id of input
    description: 'What to Do'
    required: true
    default: 'Pakage version update and commit'
runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/config.sh
      shell: bash
    - run: ${{ github.action_path }}/goodbye.sh
      shell: bash
